function task(arr){
    let longest = [];
    let i = 0;
    let command = arr[i];
    i++
    while(i < arr.length){
        let tempArr = [];
        if(command == arr[i]){
            tempArr.push(command)
        }
        if(command != arr[i])

    };
}